// Import the Flutter material library, which provides widgets and utilities for building UIs.
import 'package:flutter/material.dart';
// Import the app.dart file, which contains the root widget of your application.
import 'package:ReCall2/app.dart'; 

// This is the main function, the entry point of your Flutter application.
void main() {
  // Run the ReCall2 widget, which is the root widget of your application.
  // The const keyword indicates that the ReCall2 widget is immutable.
  runApp(const ReCall2()); 
}
